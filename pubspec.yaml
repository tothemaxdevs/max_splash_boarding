name: max_splash_boarding
description: Print dialog for thermal-printer plugin.
homepage: https://github.com/tothemaxdevs/max_splash_boarding
version: 1.6.0+1

environment:
  sdk: '>=3.2.2 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  # cached_video_player: ^2.0.4
  flutter_svg: ^2.0.5
  # flutter_cached_video_player_plus: ^2.1.3
  getwidget: ^4.0.0
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:

  uses-material-design: true

  assets:
    - assets/
